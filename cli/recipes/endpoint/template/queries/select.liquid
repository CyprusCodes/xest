const { submitQuery, camelKeys } = require("~root/lib/database");

const select{{entityName | toPascalCase}} = ({{ filterColumns | functionParameters }}) => submitQuery`
{% sql %}
SELECT {{ selectableColumns | sqlColumns: schema }} 
        FROM {{ entityName | toSnakeCase }}
        {{ filterColumns | sqlFilters  }} 
{% endsql %}
`;

module.exports = camelKeys(select{{entityName | toPascalCase}});