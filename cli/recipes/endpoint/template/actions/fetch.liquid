const select{{ entityName | toPascalCase }} = require('./queries/select{{ entityName | toPascalCase }}');

const fetch{{entityName | toPascalCase}} = async ({{ filterColumns | functionParameters }}) => {
    const {{ entityName | toCamelCase }} = await select{{ entityName | toPascalCase }}({{ filterColumns | functionParameters }});
    
    return { {{ entityName | toCamelCase }} };
}

module.exports = fetch{{entityName | toPascalCase}};