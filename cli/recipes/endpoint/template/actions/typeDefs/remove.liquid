declare module "~root/actions/{{ entityName | toCamelCase }}/remove{{ entityName | toPascalCase }}" {
    export type {{ entityName | toPascalCase }} = {
        {%- for column in filterColumnsWithType -%}
            {{ column.column | toCamelCase }}: {{ column.dataType | toTypeScriptType }};
        {%- endfor -%}
    };

    export type Remove{{ entityName | toPascalCase }}Arguments = {{ entityName | toPascalCase }};

    const remove{{ entityName | toPascalCase }}: (
        args: Remove{{ entityName | toPascalCase }}Arguments
    ) => Promise<>;

    export = remove{{ entityName | toPascalCase }};
}