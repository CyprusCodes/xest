build artefacts from `npm run build` within frontend project https://github.com/CyprusCodes/azimutt-app should be placed in this directory

these artefacts will be output to:

```
azimutt/backend/priv/static/elm
```